
module.exports = {
	load: function(){
		var TF = window.TF;

		var sideNav;
		/*
		if($('#sideNavCats').length === 0){
			sideNav = require('../components/side-nav-widget');
			sideNav.renderSync().appendTo(document.getElementById('sideNav')).getComponent();
		}else{
			sideNav = require('../components/side-nav-widget');
		}
		*/
	}
};
